@page "/tables"
@using Arinsys.Components.AspNetCore.Samples.Models
@using Arinsys.Components.AspNetCore.Samples.Fakes

<Arinsys.Components.AspNetCore.Bootstrap.Table DataAccessor="GetData" TEntity="User" TTableDataFilters="Arinsys.Components.AspNetCore.TableDataFilters<User>">
    <Head>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Email</th>
        </tr>
    </Head>
    <Body>
        <tr>
            <td>@context.Id</td>
            <td>@context.Name</td>
            <td>@context.Email</td>
        </tr>
    </Body>
</Arinsys.Components.AspNetCore.Bootstrap.Table>

@code{
    protected Task<IEnumerable<User>> GetData(Arinsys.Components.AspNetCore.TableDataFilters<User> tableDataFilters)
    {
        IEnumerable<User> users = FakeUser.ValidDefaults.Generate(10);
        return Task.FromResult(users);
    }
}